image: node:latest

stages:
  - deploy

deploy:
  stage: deploy
  script:
    - yarn install
    - yarn build
#    - ./node_modules/lerna/cli.js bootstrap --use-workspaces
    - ./node_modules/lerna/cli.js publish
